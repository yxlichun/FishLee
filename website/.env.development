# 开发环境变量
# 默认不启用远程 API，只用 localStorage（适合纯前端开发）
# 需要联调远程存储时：
#   1. 在本地启动 server/（node server/index.js，需配置 TOS 环境变量）
#   2. 将下面两项改为 true 和本地地址
VITE_API_ENABLED=false
VITE_API_BASE=http://localhost:3000
